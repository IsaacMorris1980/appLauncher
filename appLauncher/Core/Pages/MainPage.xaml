<Page
    x:Class="appLauncher.Core.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:appLauncher.Core.Helpers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:control="using:appLauncher.Core.Control"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:helper="using:appLauncher.Core.Helpers"
    xmlns:models="using:appLauncher.Core.Model"
    mc:Ignorable="d"
    Loaded="Page_Loaded"
    PointerEntered="Page_PointerEntered"
    Background="{x:Bind helper:SettingsHelper.totalAppSettings.AppBackgroundColorBrush}"
     >

    <Grid 
          Name="rootGrid" HorizontalAlignment="Stretch">

        <controls:RotatorTile x:Name="backimage" ItemsSource="{x:Bind helper:ImageHelper.backgroundImage}" Direction="Left" >
            <controls:RotatorTile.ItemTemplate>
                <DataTemplate x:DataType="models:PageBackgrounds" >
                    <Canvas Background="{x:Bind SetImage}" />
                </DataTemplate>
            </controls:RotatorTile.ItemTemplate>
        </controls:RotatorTile>



        <!--#region  All Apps-->
        <Grid Name="AppListViewGrid"
             
              Visibility="Visible">
            <Grid.RowDefinitions>
                <RowDefinition Height="100" />
                <RowDefinition Height="*"/>
                <RowDefinition Height="100" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                 >
                <RelativePanel HorizontalAlignment="Stretch" BorderThickness="2" 
                               VerticalAlignment="Stretch">
                    <AppBarButton Foreground="{x:Bind helper:SettingsHelper.totalAppSettings.AppForegroundColorBrush}"
                                  Label="Settings" LabelPosition="Default" Margin="10,20,10,0" x:Name="SettingsPage" Content="Settings" RelativePanel.AlignLeftWithPanel="True"  Tapped="SettingsPage_Tapped" Icon="Setting"
                        
                                >


                    </AppBarButton>


                    <AutoSuggestBox PlaceholderText="Search for Apps" x:Name="SearchField" RelativePanel.AlignHorizontalCenterWithPanel="True" VerticalAlignment="Top"
                                    Background="{x:Bind helper:SettingsHelper.totalAppSettings.AppBackgroundColorBrush}"
                                    Foreground="{x:Bind helper:SettingsHelper.totalAppSettings.AppForegroundColorBrush}"
                                    Margin="0,35,0,0"
                                    TextChanged="SearchField_TextChanged"
                                    SuggestionChosen="SearchField_SuggestionChosen"
                                    Width="175"
                                   >
                        <AutoSuggestBox.Resources>
                            <DataTemplate x:Key="ComboTemplate" x:DataType="models:Apps">

                                <Grid ToolTipService.ToolTip="{x:Bind Tip}" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Margin="0,10,0,10" >
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="50" />
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Canvas Grid.Column="0" Background="{x:Bind LogoBrush}" Height="50" Width="50" />
                                    <TextBlock Grid.Column="1" Text="{x:Bind Name}" 
                                                   VerticalAlignment="Center"
                                                   Margin="4,0,0,0"
                                                   FontSize="16" 
                                              TextWrapping="Wrap"
                                                   Foreground="{x:Bind TextBrush}"/>
                                </Grid>
                            </DataTemplate>
                        </AutoSuggestBox.Resources>
                        <AutoSuggestBox.ItemTemplate>
                            <StaticResource ResourceKey="ComboTemplate"/>
                        </AutoSuggestBox.ItemTemplate>

                    </AutoSuggestBox>
                    <AppBarButton Margin="10,25,10,0" RelativePanel.AlignRightWithPanel="True"  Icon="Filter" Label="Filter by" LabelPosition="Default">
                        <AppBarButton.ContextFlyout>
                            <MenuFlyout>
                                <MenuFlyoutItem Text="A-Z" />
                                <MenuFlyoutItem Text="Developer" />
                                <MenuFlyoutItem Text="Installed Date" />

                            </MenuFlyout>
                        </AppBarButton.ContextFlyout>
                    </AppBarButton>

                </RelativePanel>
            </Grid>
            <FlipView Grid.Row="1"
           Background="Transparent"
                  Margin="0,50,0,0"
                   Name="screensContainerFlipView"
                  CanDrag="True"
                     AllowDrop="True"
            ManipulationMode="All"
                SelectionChanged="FlipViewMain_SelectionChanged">
                <FlipView.Resources>
                    <DataTemplate x:Key="FlipViewMainDataTemplate">
                        <control:appControl />
                    </DataTemplate>
                </FlipView.Resources>
                <FlipView.ItemTemplate>
                    <StaticResource ResourceKey="FlipViewMainDataTemplate"/>
                </FlipView.ItemTemplate>
            </FlipView>
            <RelativePanel Grid.Row="2" HorizontalAlignment="Stretch" 
                            >
                <controls:DropShadowPanel x:Name="Dropshadow" Canvas.ZIndex="100"
                                      Margin="10"
                                          Background="{x:Bind helper:SettingsHelper.totalAppSettings.AppBackgroundColorBrush}"
                                          Foreground="{x:Bind helper:SettingsHelper.totalAppSettings.AppForegroundColorBrush}"
                        VerticalAlignment="Bottom"
                        HorizontalAlignment="Center"
                                      HorizontalContentAlignment="Stretch"
                                      VerticalContentAlignment="Stretch"
                                      ShadowOpacity="0.2"
                                      BlurRadius="10"
                                      RelativePanel.AlignTopWithPanel="True"
                                        
                                     >


                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="50" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="60" />
                        </Grid.RowDefinitions>
                        
                        <AppBarButton x:Name="PreviousPage" Icon="Back" Width="40" Height="30" Tapped="PreviousPage_Tapped"
                              Grid.Row="0" Grid.Column="0"
                                      IsEnabled="{x:Bind helper:packageHelper.pageVariables.IsPrevious}"/>
                        <ListView x:Name="listView" ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                  ScrollViewer.HorizontalScrollBarVisibility="Visible"
                                  ScrollViewer.IsHorizontalRailEnabled="True"
                                  ScrollViewer.HorizontalScrollMode="Auto"
                                  ScrollViewer.VerticalScrollMode="Disabled"
                                  VerticalAlignment="Top"
                                  Grid.Column="1"
                                  >
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>

                        </ListView>
                        <AppBarButton x:Name="NextPage" Icon="Forward" Width="40" Height="30" Tapped="NextPage_Tapped"
                              RelativePanel.AlignRightWithPanel="True" RelativePanel.RightOf="Dropshadow"
                                      IsEnabled="{x:Bind helper:packageHelper.pageVariables.IsNext}"
                                      Grid.Row="0"
                                      Grid.Column="2"/>
                    </Grid>

                  
                 
                    
                    <!--<AppBar Background="{x:Bind helper:SettingsHelper.totalAppSettings.AppBackgroundColorBrush}" >
                        <ScrollViewer Width="300" >
                            <StackPanel Name="flipViewIndicatorStackPanel"
                        Orientation="Horizontal"
                            Background="{x:Bind helper:SettingsHelper.totalAppSettings.AppBackgroundColorBrush}"
                        
                       />
                        </ScrollViewer>
                    </AppBar>-->

                </controls:DropShadowPanel>
                

                <StackPanel Orientation="Horizontal" RelativePanel.Below="Dropshadow" RelativePanel.AlignHorizontalCenterWithPanel="True"  Margin="0" >
                    <!--<TextBlock Text="Filter By" Width="55" Margin="20" TextAlignment="Right" Foreground="{x:Bind helper:SettingsHelper.totalAppSettings.AppForegroundColorBrush}"
                               />
                    <ComboBox Background="{x:Bind helper:SettingsHelper.totalAppSettings.AppBackgroundColorBrush}"
                              Foreground="{x:Bind helper:SettingsHelper.totalAppSettings.AppForegroundColorBrush}" Name="Filterby" Margin="20" Width="150" SelectionChanged="Filterby_SelectionChanged">
                        <ComboBoxItem Content="AtoZ" />
                        <ComboBoxItem Content="Developer" />
                        <ComboBoxItem Content="Installed" />
                    </ComboBox>-->
                   
                </StackPanel>

            </RelativePanel>

        </Grid>
        <!--#endregion-->





    </Grid>
</Page>