 ﻿<ContentDialog
    x:Class="appLauncher.Core.Pages.EnableFeatures"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:appLauncher.Core.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="TITLE"
    PrimaryButtonText="Update Settings"
    SecondaryButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick"
    Loaded="ContentDialog_Loaded"
   >

    <Grid>
        <ScrollViewer Height="200">
            <StackPanel Orientation="Vertical">
                <CheckBox x:Name="EnableSearch" Content="Enable Searching for apps" />
                <CheckBox x:Name="EnableFilter" Content="Enable Filtering apps" />
                <CheckBox x:Name="EnableTiles" Content="Enable App Tiles Settings" />
                <CheckBox x:Name="EnableAppSettings" Content="Enable Application Settings" />
                <CheckBox x:Name="EnableImages" Content="Enable Background Images" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</ContentDialog>
